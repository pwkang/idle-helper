{
  "name": "@idle-helper/services",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build:shared": "tsc",
    "type-check": "tsc --noEmit --skipLibCheck",
    "lint": "eslint src/ --ext .ts --fix"
  },
  "dependencies": {
    "@idle-helper/utils": "workspace:*"
  },
  "devDependencies": {
    "@idle-helper/tsconfig": "workspace:*"
  }
}
